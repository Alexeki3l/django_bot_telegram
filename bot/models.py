from django.db import models
# from django.contrib.auth.models import User


# from tienda.models import Tienda, Producto
# # Create your models here.


# class Notification(models.Model):
#     id_user     = models.ForeignKey(User, on_delete=models.CASCADE)
#     # id_tienda = models.ForeignKey(Tienda, on_delete=models.CASCADE, null=True, blank= True)
#     id_product  = models.ForeignKey(Producto, on_delete=models.CASCADE, null=True, blank= True)
#     type        = models. 